<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:FrictionCircle.CustomViews"
             x:Class="FrictionCircle.Views.MainPage">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <local:FrictionCircleView XAcc="{Binding XAcc}" YAcc="{Binding YAcc}" />
        <Button Grid.Row="1" Text="Calibrate" Command="{Binding CalibrateCommand}" />
    </Grid>

</ContentPage>